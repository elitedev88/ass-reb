# Backend API URL
# For local development with the PHP backend
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# For production or if running without the PHP backend
# The app will show a message that the backend is not configured
# NEXT_PUBLIC_API_URL=https://your-api-domain.com/api
